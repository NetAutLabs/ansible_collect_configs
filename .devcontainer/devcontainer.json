{
    "image": "ghcr.io/ipspace/netlab/devcontainer:1.8.4",
    "hostRequirements": {
        "cpus": 2,
        "memory": "8gb",
        "storage": "32gb"
    },
    "postStartCommand": "sudo apt update && sudo apt install -y --no-install-recommends linux-headers-generic && sudo netlab install -y grpc"
}